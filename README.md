# Frontend

React + TypeScript + Vite client for the PrepareForTraining Management API.

## Start here

1. Copy `.env.example` to `.env`.
2. Run `npm install`.
3. Run `npm run dev`.
4. Implement pages and services following `../docs/API_CONTRACT.md` and `../docs/IMPLEMENTATION_PLAN.md`.
