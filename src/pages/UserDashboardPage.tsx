import { AppShell } from "../components/common/AppShell";
export function UserDashboardPage() {
  return (
    <AppShell>
      <main>
        <h1>User dashboard</h1>
        <p>
          Your account is ready. Project documents will be available after you
          are added to a project.
        </p>
      </main>
    </AppShell>
  );
}
